tasks:
  - before: nvm install
    init: yarn
    command: yarn start |
      export HMR_HOST=`gp url 3000`

ports:
  - port: 3000
    visibility: public
    name: book-browser
    onOpen: open-preview

vscode:
  extensions:
    - dbaeumer.vscode-eslint